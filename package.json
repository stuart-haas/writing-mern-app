{
  "name": "node-docker",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "build": "docker-compose build",
    "restart": "docker-compose restart",
    "stop": "docker-compose stop",
    "kill": "docker-compose kill",
    "dev": "yarn kill && docker-compose -f docker-compose.yml -f docker-compose.dev.yml up --build --remove-orphans",
    "prod": "yarn kill && docker-compose -f docker-compose.yml -f docker-compose.prod.yml up --build --remove-orphans"
  },
  "author": "Stuart Haas",
  "license": "ISC"
}
